# Only needed when the client runs on the Vite dev server and the API is on a
# different port. In the Render deployment the API serves this bundle from the
# same origin, so VITE_API_URL is left unset and requests go to /api.
VITE_API_URL=http://localhost:4000/api
